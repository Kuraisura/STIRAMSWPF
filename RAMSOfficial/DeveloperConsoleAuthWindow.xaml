<Window x:Class="RAMSOfficial.DeveloperConsoleAuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Developer Console Access"
        Height="230"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        WindowStyle="SingleBorderWindow"
        Topmost="True"
        KeyDown="Window_KeyDown">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   Text="Developer Console"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#0F172A"/>
        <TextBlock x:Name="SubtitleText"
                   Grid.Row="1"
                   Text="Enter password to continue"
                   Margin="0,6,0,0"
                   Foreground="#475569"/>

        <PasswordBox x:Name="PasswordInput"
                     Grid.Row="2"
                     Margin="0,14,0,0"
                     FontSize="16"
                     Padding="10,8"
                     KeyDown="PasswordInput_KeyDown"/>

        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Margin="0,8,0,0"
                   Foreground="#DC2626"
                   Visibility="Collapsed"
                   Text="Incorrect password."/>

        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Cancel" Padding="16,7" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button x:Name="UnlockButton" Grid.Column="2" Content="Unlock" Padding="16,7" Click="UnlockButton_Click"/>
        </Grid>
    </Grid>
</Window>
